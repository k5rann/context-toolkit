import { BundlerPage } from "@/components/bundler/bundler-page";

export const metadata = {
  title: "Context Bundler — Context Toolkit",
};

export default function Page() {
  return <BundlerPage />;
}
